function insertParagraph( paragraph, id ) {
	var element = document.getElementById(id);
	paragraph.addElement( paragraph );
}

function addLineBreaks( lines ) {
  var paragraph;
  return paragraph;
}

function addIndents( paragraph ) {
  return paragraph;
}

window.onload = function() {
  var client =
  var memoirPoemLines;
  insertParagraph( addLineBreaks( memoirPoemLines ), 'memoirpoem' );

  var masqueradeShortStoryLines;
  insertParagraph( addIndents( addLineBreaks( masqueradeShortStoryLines ) ), 'masqueradeshortstory' );
}
